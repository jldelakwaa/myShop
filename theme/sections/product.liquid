{% comment %}
  This section is used in the product template to render product page with
  media, content, and add-to-cart form.

  https://shopify.dev/docs/storefronts/themes/architecture/templates/product
{% endcomment %}

{% liquid
  assign deck_guide_query = product.title | append: ' structure deck guide how to play' | url_encode
%}

<section
  class="arcana-product full-width"
  style="--product-bg-image: url({% if product.featured_image %}{{ product.featured_image | image_url: width: 1800 }}{% else %}{{ 'arcana-vault-hero.png' | asset_url }}{% endif %});"
>
  <div class="arcana-product__inner">
    <div class="product-gallery">
      <div class="product-gallery__heading">
        <p class="arcana-eyebrow">Vault record</p>
        <span>{{ product.type | default: 'Structure Deck' }}</span>
      </div>

      <div class="product-images">
      {% if product.images.size > 0 %}
        {% for image in product.images %}
          {% render 'image', class: 'product-image', image: image %}
        {% endfor %}
      {% else %}
        <div
          class="product-image product-image--placeholder"
          style="background-image: linear-gradient(180deg, rgba(18, 13, 27, 0.08), rgba(18, 13, 27, 0.82)), url({{ 'arcana-vault-hero.png' | asset_url }});"
        >
          <span>{{ product.title }}</span>
        </div>
      {% endif %}
      </div>
    </div>

    <div class="product-panel">
      <p class="arcana-eyebrow">{{ product.vendor | default: 'Arcana Vault' }}</p>
      <h1>{{ product.title }}</h1>
      <div class="product-panel__meta">
        <p class="product-price">{{ product.price | money }}</p>
        {% if product.available %}
          <span>Ready to sleeve</span>
        {% else %}
          <span>Out of stock</span>
        {% endif %}
      </div>
      <div class="product-description">{{ product.description }}</div>

      <div class="product-guide">
        <span>Learn the line</span>
        <p>Open current YouTube guides for this deck before you buy or sleeve it.</p>
        <a
          href="https://www.youtube.com/results?search_query={{ deck_guide_query }}"
          target="_blank"
          rel="noreferrer"
        >
          Watch deck guide
        </a>
      </div>

      <div class="product-buy-plan">
        <span>Recommended buy plan</span>
        <h2>Most duelists start with 3 copies</h2>
        <p>
          One copy is fine for casual testing. Three copies gives you a stronger pool of core engine cards,
          staple reprints, and playset ratios for tuning a real deck list.
        </p>
        <button type="button" data-quantity-preset="3">Set quantity to 3</button>
      </div>

      <div class="product-promises">
        <span><strong>Build type</strong> Archetype-ready deck lane</span>
        <span><strong>Vault care</strong> Collector-safe packaging</span>
        <span><strong>Play path</strong> Tuned for fast table setup</span>
      </div>

      <div class="product-form">
        {% form 'product', product %}
          {% assign current_variant = product.selected_or_first_available_variant %}

          <div class="product-form__row">
            <label for="ProductVariant-{{ section.id }}">Edition</label>
            <select id="ProductVariant-{{ section.id }}" name="id">
              {% for variant in product.variants %}
                <option
                  value="{{ variant.id }}"
                  {% if variant == current_variant %}
                    selected="selected"
                  {% endif %}
                >
                  {{ variant.title }} - {{ variant.price | money }}
                </option>
              {% endfor %}
            </select>
          </div>

          <div class="product-form__row">
            <label for="ProductQuantity-{{ section.id }}">Quantity</label>
            <input id="ProductQuantity-{{ section.id }}" type="number" name="quantity" min="1" value="1">
            <div class="product-quantity-presets" aria-label="Quantity presets">
              <button type="button" data-quantity-preset="1">1 casual copy</button>
              <button type="button" data-quantity-preset="3">3 deck-building copies</button>
            </div>
          </div>

          <input type="submit" value="Add to vault">
          {{ form | payment_button }}
        {% endform %}
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const productSection = document.querySelector(".arcana-product");
    if (!productSection) return;

    const quantityInput = productSection.querySelector("input[name='quantity']");
    const presetButtons = productSection.querySelectorAll("[data-quantity-preset]");

    presetButtons.forEach((button) => {
      button.addEventListener("click", () => {
        if (!quantityInput) return;

        quantityInput.value = button.dataset.quantityPreset || "1";
        quantityInput.dispatchEvent(new Event("change", { bubbles: true }));
      });
    });
  })();
</script>

{% stylesheet %}
  .arcana-product {
    position: relative;
    overflow: hidden;
    padding: clamp(56px, 8vw, 104px) var(--page-margin);
    background:
      linear-gradient(90deg, rgba(20, 15, 31, 0.94), rgba(20, 15, 31, 0.66) 56%, rgba(255, 214, 107, 0.16)),
      linear-gradient(180deg, rgba(20, 15, 31, 0.5), rgba(20, 15, 31, 0.92)),
      var(--product-bg-image);
    background-position: center;
    background-size: cover;
  }

  .arcana-product::before {
    content: "";
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgba(255, 247, 223, 0.055) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 247, 223, 0.045) 1px, transparent 1px);
    background-size: 52px 52px;
    mask-image: linear-gradient(90deg, #000, transparent 74%);
    pointer-events: none;
  }

  .arcana-product__inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(320px, 0.92fr);
    gap: clamp(24px, 5vw, 64px);
    width: min(var(--page-width), calc(100vw - var(--page-margin) * 2));
    margin: 0 auto;
  }

  .arcana-product .arcana-eyebrow {
    color: #6a58d8;
    font-size: 0.84rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .product-gallery {
    display: grid;
    gap: 18px;
    align-self: start;
    padding: clamp(18px, 3vw, 30px);
    border: 1px solid rgba(255, 214, 107, 0.28);
    border-radius: 8px;
    background:
      linear-gradient(135deg, rgba(255, 214, 107, 0.18), transparent 34%),
      rgba(10, 7, 17, 0.72);
    backdrop-filter: blur(18px);
    color: #fff7df;
  }

  .product-gallery__heading {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }

  .product-gallery__heading .arcana-eyebrow {
    color: #ffd66b;
  }

  .product-gallery__heading span {
    color: rgba(255, 247, 223, 0.72);
    font-size: 0.9rem;
  }

  .product-images {
    display: grid;
    gap: 14px;
  }

  .product-image,
  .product-image img {
    overflow: hidden;
    border-radius: 8px;
    background: #21172f;
  }

  .product-image img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
  }

  .product-image--empty {
    display: grid;
    min-height: 520px;
    place-items: center;
    color: #6b627c;
  }

  .product-image--placeholder {
    display: grid;
    min-height: 520px;
    place-items: end start;
    padding: 28px;
    background-position: center;
    background-size: cover;
    color: #fff7df;
  }

  .product-image--placeholder span {
    max-width: 12ch;
    font-size: clamp(2rem, 5vw, 4.5rem);
    font-weight: 800;
    line-height: 0.98;
  }

  .product-panel {
    position: sticky;
    top: 24px;
    align-self: start;
    padding: 28px;
    border: 1px solid rgba(106, 88, 216, 0.2);
    border-radius: 8px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.94), rgba(251, 250, 245, 0.94)),
      #fff;
    backdrop-filter: blur(16px);
    box-shadow: 0 24px 70px rgba(28, 20, 41, 0.12);
  }

  .product-panel h1 {
    margin-top: 10px;
    color: #140f1f;
    font-size: clamp(2.2rem, 4vw, 4.5rem);
    line-height: 0.98;
  }

  .product-panel__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 12px;
    margin-top: 18px;
  }

  .product-price {
    font-size: 1.35rem;
    font-weight: 700;
  }

  .product-panel__meta span {
    padding: 7px 10px;
    border: 1px solid #d8d1f0;
    border-radius: 999px;
    color: #493d5f;
    font-size: 0.84rem;
    font-weight: 700;
  }

  .product-description {
    margin-top: 20px;
    color: #5f5870;
    line-height: 1.65;
  }

  .product-guide {
    display: grid;
    gap: 8px;
    margin-top: 20px;
    padding: 16px;
    border: 1px solid rgba(255, 214, 107, 0.42);
    border-radius: 8px;
    background:
      linear-gradient(135deg, rgba(255, 214, 107, 0.2), transparent 38%),
      #140f1f;
    color: #fff7df;
  }

  .product-guide span {
    color: #ffd66b;
    font-size: 0.78rem;
    font-weight: 800;
    text-transform: uppercase;
  }

  .product-guide p {
    color: rgba(255, 247, 223, 0.76);
    line-height: 1.5;
  }

  .product-guide a {
    color: #ffd66b;
    font-weight: 800;
  }

  .product-buy-plan {
    display: grid;
    gap: 8px;
    margin-top: 14px;
    padding: 16px;
    border: 1px solid #d8d1f0;
    border-radius: 8px;
    background: #fff;
  }

  .product-buy-plan span {
    color: #6a58d8;
    font-size: 0.78rem;
    font-weight: 800;
    text-transform: uppercase;
  }

  .product-buy-plan h2 {
    color: #140f1f;
    font-size: 1.18rem;
    line-height: 1.2;
  }

  .product-buy-plan p {
    color: #5f5870;
    line-height: 1.55;
  }

  .product-buy-plan button {
    justify-self: start;
    min-height: 40px;
    padding: 0 14px;
    border-color: #ffd66b;
    background: #ffd66b;
    color: #140f1f;
  }

  .product-promises {
    display: grid;
    gap: 8px;
    margin: 22px 0;
    color: #21172f;
  }

  .product-promises span {
    display: grid;
    gap: 2px;
    padding: 12px;
    border: 1px solid #ded8f5;
    border-radius: 4px;
    background: #fbfaf5;
  }

  .product-promises strong {
    color: #6a58d8;
    font-size: 0.78rem;
    text-transform: uppercase;
  }

  .product-form form {
    display: grid;
    gap: 12px;
  }

  .product-form__row {
    display: grid;
    gap: 6px;
  }

  .product-quantity-presets {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .product-quantity-presets button {
    min-height: 36px;
    padding: 0 12px;
    border: 1px solid #d8d1f0;
    border-radius: 999px;
    background: #fff;
    color: #140f1f;
    font-size: 0.84rem;
    font-weight: 800;
  }

  .product-form label {
    color: #60675e;
    font-size: 0.86rem;
    font-weight: 700;
  }

  .product-form input,
  .product-form select {
    width: 100%;
    padding: 0 12px;
  }

  .product-form input[type="submit"] {
    min-height: 48px;
    border: 1px solid #140f1f;
    border-radius: 4px;
    background: #140f1f;
    color: #fff7df;
    font-weight: 800;
    cursor: pointer;
  }

  @media (max-width: 900px) {
    .arcana-product__inner {
      grid-template-columns: 1fr;
    }

    .product-panel {
      position: static;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.product",
  "settings": [],
  "disabled_on": {
    "groups": ["header", "footer"]
  }
}
{% endschema %}
